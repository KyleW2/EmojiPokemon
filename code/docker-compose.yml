version: '3.7'

services:
  server:
    build: .
    hostname: server
    container_name: Server
    networks:
      - default
  client0:
    build: .
    hostname: trainer0
    container_name: trainer0
    networks:
      - default
  client1:
    build: .
    hostname: pokemon0
    container_name: pokemon0
    networks:
      - default
  client2:
    build: .
    hostname: pokemon1
    container_name: pokemon1
    networks:
      - default
networks:
  default:
    driver: bridge